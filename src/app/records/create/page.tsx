import Form from "@/app/records/create/components/Form";

export default function PageCreate() {
    return (
        <Form
            cardDescription="Create a new Brand"
            cardTitle="Create Brand"
            textButton="Create"
        />
    );
}
